{
  "name": "mydandefi-subgraph-test",
  "license": "UNLICENSED",
  "scripts": {
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mydandefi-subgraph-test",
    "deploy-bsc":"graph deploy --product hosted-service stitetsati/mydandefi-bnb",
    "create-local": "graph create --node http://localhost:8020/ mydandefi-subgraph-test",
    "remove-local": "graph remove --node http://localhost:8020/ mydandefi-subgraph-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mydandefi-subgraph-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2",
    "prettier": "^3.0.3"
  },
  "prettier": {
    "printWidth": 200
  }
}
